
// //Returns tokens from the API and user added
// export const useAllTokens = () => {
//         const xlmToken = xlmTokenList.find((set) => set.network === network)?.assets

//   const { tokensAsMap: apiTokensAsMap, tokens: apiTokens, isLoading } = useNormalTokens();

//   return {
//     tokensAsMap: { ...apiTokensAsMap, ...userAddedTokensAsMap },
//     tokens: [...apiTokens, ...userTokens],
//     isLoading,
//   };
// };
