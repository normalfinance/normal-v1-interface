import type { ConfettiConfig } from './types';

// ----------------------------------------------------------------------
export const defaultConfettiConfig: ConfettiConfig = {
  numberOfPieces: 500,
  recycle: false,
  run: false,
  duration: 5000,
};
