export default [
  {
    // Folders ESLint should ignore
    ignores: ['**/dist/**', '**/.next/**', '**/node_modules/**'],

    // You can extend Next or other presets here if you like
    // Example:
    // plugins: { next: (await import('eslint-plugin-next')).default },
    // rules: { ... }

    // For now keep it empty; individual packages can extend further
  },
];
